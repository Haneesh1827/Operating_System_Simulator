# Operating System- BasicSimulator

<strong>Running instructions : </strong>
Clone or download the repository. Compile by running the make command in your terminal:

```make```<br>
```./.my_os_simulator```
